### This file was generated by Nexus Schema
### Do not make changes to this file directly


type AuthPayload {
  token: String
  user: User
}

type Book {
  author: String!
  condition: String!
  description: String
  id: Int!
  ISBN: String
  owner: User
  rating: Int
  status: String!
  title: String!
  user: User
}

type Mutation {
  addBook(author: String!, categoryId: Int!, condition: String!, description: String!, ISBN: String!, languageId: Int!, status: String!, title: String!): Book
  checkout(bookId: Int!): Book
  createOrder(bookId: Int!, quantity: Int!, unitPrice: Float!): Order
  deleteBook(id: Int!): Book
  login(email: String!, password: String!): AuthPayload
  signup(email: String!, name: String, password: String!): AuthPayload
}

type Order {
  book: Book
  id: Int!
  quantity: Int!
  unitPrice: Float!
  user: User
}

type OrderResp

type Query {
  filterBooks(searchString: String): [Book]
  getAvailableBooks: [Book]
  getOrder(orderId: Int!): Order
  me: User
}

type User {
  books: [Book!]!
  email: String!
  id: Int!
  name: String
}
